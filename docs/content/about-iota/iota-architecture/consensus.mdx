---
title: Consensus on IOTA
sidebar_label: Consensus 
description: Overview of the IOTA consensus.
tags: [core-protocol]
---

The primary goal of consensus in distributed systems is to establish a consistent order of transactions and ensure their availability. In IOTA, consensus is achieved through a combination of Delegated Proof-of-Stake (dPoS) and the Mysticeti Byzantine Fault Tolerant (BFT) consensus protocol.

IOTA's consensus mechanism ensures that:

- Users submit transactions to validators.
- Validators execute the Mysticeti protocol to reach agreement on the order and finalization of transactions.
- The result is a consistently ordered ledger that is agreed upon by an honest majority of validators.

## Delegated Proof-of-Stake (dPoS) in IOTA

IOTA employs Delegated Proof-of-Stake (dPoS) as the overarching consensus mechanism. In this model:

- Token holders delegate their voting power to validators.
- Validators participate in block production and execute the Mysticeti protocol to finalize transactions.
- This structure ensures security and decentralization while maintaining scalability.

## The Mysticeti Protocol

The Mysticeti protocol is a leaderless BFT protocol optimized for low-latency and high-throughput using an uncertified DAG structure. It is designed to facilitate efficient and parallel transaction processing while maintaining strong security guarantees.

### Key features of Mysticeti include:

- Parallel Block Proposals: Multiple validators can propose blocks simultaneously, maximizing network bandwidth and ensuring censorship resistance. This is a core advantage of DAG-based protocols.
- Three-Round Commit Rule: Transactions achieve finality in just three rounds of messages, matching the efficiency of pBFT and reaching the theoretical minimum for BFT consensus.
- Optimized Leaderless Voting: Unlike traditional leader-based protocols, Mysticeti allows validators to vote on blocks and certify them in parallel, significantly reducing median and tail latencies.
- Fault Tolerance: The protocol tolerates unavailable or faulty validators without significantly increasing commit latencies.

For more technical details, including correctness proofs, refer to the [Mysticeti paper](https://arxiv.org/pdf/2310.14821).

## Clarifications and Terminology

- Consensus Outputs: Consensus does not "output" anything directly; instead, it establishes a globally agreed-upon order of transactions.

- Consistently Ordered Transactions: The consensus ensures that transactions are ordered consistently among the honest majority of validators, preventing double-spending and conflicts.

- Validators' Role: Validators accept transactions, execute the consensus protocol, and produce a consistently ordered ledger state.

- Well-Behaving Validators: Honest validators follow the protocol correctly, contributing to the security and stability of the network. However, the protocol is designed to function even if some validators are malicious or unavailable.

